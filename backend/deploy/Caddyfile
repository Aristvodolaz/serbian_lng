# Reverse proxy + automatic HTTPS (Let's Encrypt) for API and Admin Panel.
# Replace the domains below with your actual domains.
# Caddy auto-provisions and renews Let's Encrypt certs.

api.example.com {
    reverse_proxy app:3000
}

admin.example.com {
    reverse_proxy admin:3001
}
